# Dependencias de produccion de Northwind Enterprise API.
# Solo dependencias directas: las transitivas las resuelve pip.
#
# Generadas a partir de los imports reales de app/ y tests/.
# Instalacion:  pip install -r requirements.txt
# Instalacion con las herramientas de desarrollo/CI:  pip install -r requirements-ci.txt

# --- Framework web ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# --- Validacion y configuracion ---
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-dotenv==1.2.2

# --- Persistencia ---
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4

# --- Cache ---
redis==8.1.0

# --- Seguridad ---
bcrypt==5.0.0
python-jose==3.5.0
